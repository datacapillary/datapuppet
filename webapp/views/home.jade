extends layout

block content
  .page-header
    h3 !{title}
    a.btn.btn-success.pull-right(href='/host/create') Add

  table.table.table-stripe#host-table
    thead.thead-inverse
      tr
        th Host Id
        th Domain
        th Priority
        th Actions
    tbody
      each host in hosts
        tr
          td.col-md-4
            a(href="/host/" + host.id + "/edit") !{host.id}
          td.col-md-3= host.domain
          td.col-md-2= host.setting.priority
          td.col-md-3
            form.form-group(action='/host/delete', method='POST')
              input(type='hidden', name='host_id', value=host.id)
              button.btn-xs.btn-danger(type='submit')
                i.fa.fa-trash
                | Delete

